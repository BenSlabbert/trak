spring:
  config:
    name: trak-engine
  liquibase:
    change-log: classpath:liquibase.sql
  jpa:
    show-sql: true
  rabbitmq:
    username: trak
    password: password
    host: localhost
    virtual-host: /
    port: 5672
    listener:
      simple:
        concurrency: 2
        max-concurrency: 2
  redis:
    host: localhost
    port: 6379
    ssl: false
