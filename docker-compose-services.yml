version: '2.4'

services:

  rabbit:
    image: rabbitmq:3.7.14-management-alpine
    hostname: rabbit
    restart: always
    mem_limit: 600m
    environment:
      - RABBITMQ_DEFAULT_USER=trak
      - RABBITMQ_DEFAULT_PASS=password
      - RABBITMQ_DEFAULT_VHOST=/
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
      - rabbit-data:/var/lib/rabbitmq

  redis:
    image: redis:5.0.4-alpine
    restart: always
    ports:
      - 6379:6379
    mem_limit: 300m
    volumes:
      - redis-data:/data

  rds:
    image: mariadb:10.3.14
    restart: always
    ports:
      - 3306:3306
    volumes:
      - rds-data:/var/lib/mysql
    mem_limit: 300m
    environment:
      - MYSQL_ROOT_PASSWORD=my-secret-pw
      - MYSQL_DATABASE=trak
      - MYSQL_USER=user
      - MYSQL_PASSWORD=password

  es:
    image: docker.elastic.co/elasticsearch/elasticsearch:6.4.1
    restart: always
    ports:
      - 9200:9200
      - 9300:9300
    volumes:
      - es-data:/usr/share/elasticsearch/data
    environment:
      - "discovery.type=single-node"
      - "transport.host=es"
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"

  kibana:
    image: docker.elastic.co/kibana/kibana:6.4.1
    restart: always
    ports:
      - 5601:5601
    volumes:
      - kibana-data:/usr/share/kibana
    environment:
      SERVER_NAME: kibana.local
      ELASTICSEARCH_URL: http://es:9200
    depends_on:
      - es

  logstash:
    build: ./logstash-conf/
    image: benjaminslabbert/trak_logstash:1.0.0
    ports:
      - 9600:9600
    restart: always
    environment:
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    depends_on:
      - es

  ui:
    image: benjaminslabbert/trak_ui:1.0.0
    ports:
      - 3000:3000
    mem_limit: 250m

volumes:
  rabbit-data:
    driver: local
  rds-data:
    driver: local
  es-data:
    driver: local
  kibana-data:
    driver: local
  redis-data:
    driver: local
